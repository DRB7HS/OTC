local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 180, 0, 180)
MainFrame.Position = UDim2.new(1, -200, 0, 50)
MainFrame.BackgroundColor3 = Color3.fromRGB(245, 245, 245)
MainFrame.BackgroundTransparency = 0.15
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = MainFrame

local BackgroundImage = Instance.new("ImageLabel")
BackgroundImage.Name = "BackgroundImage"
BackgroundImage.Size = UDim2.new(1, 0, 1, 0)
BackgroundImage.BackgroundTransparency = 1
BackgroundImage.Image = "rbxthumb://type=Asset&w=420&h=420&id=92711879389010"
BackgroundImage.ImageTransparency = 0.6
BackgroundImage.Parent = MainFrame

local BackgroundCorner = UICorner:Clone()
BackgroundCorner.Parent = BackgroundImage

local function CreateAnimatedStroke(Object)
    local Stroke = Instance.new("UIStroke")
    Stroke.Parent = Object
    Stroke.Color = Color3.fromRGB(255,255,255)
    Stroke.Thickness = 3
    Stroke.Transparency = 0
    Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    Stroke.LineJoinMode = Enum.LineJoinMode.Round

    local Gradient = Instance.new("UIGradient")
    Gradient.Parent = Stroke
    Gradient.Rotation = 0

    Gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(10,10,10)),
        ColorSequenceKeypoint.new(0.15, Color3.fromRGB(30,30,30)),
        ColorSequenceKeypoint.new(0.35, Color3.fromRGB(255,255,255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255,255,255)),
        ColorSequenceKeypoint.new(0.65, Color3.fromRGB(255,255,255)),
        ColorSequenceKeypoint.new(0.85, Color3.fromRGB(30,30,30)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(10,10,10))
    }

    local AnimationInfo = TweenInfo.new(7, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1)
    local Track = TweenService:Create(Gradient, AnimationInfo, {Rotation = 360})
    Track:Play()
end

CreateAnimatedStroke(MainFrame)

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Position = UDim2.new(0, 0, 0, 5)
Title.BackgroundTransparency = 1
Title.Text = "     7Z HUB"
Title.TextColor3 = Color3.fromRGB(30, 30, 30)
Title.TextSize = 14
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

local StarEmoji = Instance.new("TextLabel")
StarEmoji.Name = "StarEmoji"
StarEmoji.Size = UDim2.new(0, 50, 0, 50)
StarEmoji.Position = UDim2.new(0.5, -25, 0.5, -10)
StarEmoji.BackgroundTransparency = 1
StarEmoji.Text = ""
StarEmoji.TextSize = 35
StarEmoji.AnchorPoint = Vector2.new(0.5, 0.5)
StarEmoji.Position = UDim2.new(0.5, 0, 0.5, 10)
StarEmoji.Parent = MainFrame

local StarInfo = TweenInfo.new(3, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1)
TweenService:Create(StarEmoji, StarInfo, {Rotation = 360}):Play()

local RightIconFrame = Instance.new("ImageLabel")
RightIconFrame.Name = "RightIconFrame"
RightIconFrame.Size = UDim2.new(0, 25, 0, 25)
RightIconFrame.Position = UDim2.new(1, -33, 0, 8)
RightIconFrame.BackgroundTransparency = 1
RightIconFrame.Image = "rbxthumb://type=Asset&w=420&h=420&id=92711879389010"
RightIconFrame.ImageTransparency = 0.3
RightIconFrame.Parent = MainFrame

local ContainerCorner = Instance.new("UICorner")
ContainerCorner.CornerRadius = UDim.new(0, 6)
ContainerCorner.Parent = RightIconFrame
CreateAnimatedStroke(RightIconFrame)

local LeftButtonsFrame = Instance.new("ImageLabel")
LeftButtonsFrame.Name = "LeftButtonsFrame"
LeftButtonsFrame.Size = UDim2.new(0, 55, 0, 25)
LeftButtonsFrame.Position = UDim2.new(0, 8, 0, 8)
LeftButtonsFrame.BackgroundTransparency = 1
LeftButtonsFrame.Image = "rbxthumb://type=Asset&w=420&h=420&id=92711879389010"
LeftButtonsFrame.ImageTransparency = 0.5
LeftButtonsFrame.Parent = MainFrame

local ButtonsCorner = Instance.new("UICorner")
ButtonsCorner.CornerRadius = UDim.new(0, 6)
ButtonsCorner.Parent = LeftButtonsFrame
CreateAnimatedStroke(LeftButtonsFrame)

local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Size = UDim2.new(0, 20, 0, 20)
CloseBtn.Position = UDim2.new(0, 4, 0, 2.5)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(30, 30, 30)
CloseBtn.TextSize = 12
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = LeftButtonsFrame

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleBtn"
ToggleBtn.Size = UDim2.new(0, 20, 0, 20)
ToggleBtn.Position = UDim2.new(0, 28, 0, 2.5)
ToggleBtn.BackgroundTransparency = 1
ToggleBtn.Text = "-"
ToggleBtn.TextColor3 = Color3.fromRGB(30, 30, 30)
ToggleBtn.TextSize = 14
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.Parent = LeftButtonsFrame

local OpenSound = Instance.new("Sound")
OpenSound.SoundId = "rbxassetid://9114223193"
OpenSound.Volume = 1
OpenSound.Parent = SoundService
OpenSound:Play()

local IsMinimized = false
ToggleBtn.MouseButton1Click:Connect(function()
    if not IsMinimized then
        IsMinimized = true
        ToggleBtn.Text = "+"
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 180, 0, 40)}):Play()
        BackgroundImage.Visible = false
        StarEmoji.Visible = false
    else
        IsMinimized = false
        ToggleBtn.Text = "-"
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 180, 0, 180)}):Play()
        BackgroundImage.Visible = true
        StarEmoji.Visible = true
    end
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)
